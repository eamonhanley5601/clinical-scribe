# Local development only. In production (APP_ENV=aws) these come from AWS Secrets
# Manager instead -- see app/config.py. Never commit a real .env.
DATABASE_URL=postgresql+psycopg://scribe:scribe@localhost:5432/clinical_scribe
JWT_SECRET=dev-only-change-me
OPENROUTER_API_KEY=sk-or-v1-your-key-here
LLM_MODEL=openai/gpt-oss-20b:free
CORS_ORIGINS=["http://localhost:5173"]
